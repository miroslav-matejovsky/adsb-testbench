// Package cli documents the planned internal/cli package.
//
// Shared command input validation: explicit listen addresses and
// configuration. Each command owns its flags, logger, and process lifecycle.
//
// This is a documentation-only skeleton; implementation is planned.
package cli

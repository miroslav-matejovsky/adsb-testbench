# Commands

Skeletons for the combined test bench, standalone simulator, and standalone
display. Each package documents its purpose in `doc.go`. Entry points are tracked
in the [UI and commands plan](../docs/plans/08-ui-and-commands/README.md),
with command configuration and lifecycle in step 10.
